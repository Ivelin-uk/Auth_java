# React Frontend - Инструкции за инсталация и стартиране

## 📋 Предварителни изисквания

- Node.js (версия 16 или по-нова)
- npm (обикновено идва с Node.js)
- Работещи Auth Service (port 8081) и Admin Service (port 8082)

## 🚀 Инсталация

### 1. Инсталирайте зависимостите

```bash
cd frontend
npm install
```

Това ще инсталира всички необходими пакети:
- React 18
- React Router v6
- Axios
- React Toastify

## ▶️ Стартиране

### Стартирайте development сървъра

```bash
npm start
```

Приложението ще се отвори автоматично на http://localhost:3000

## 🏗️ Структура на проекта

```
frontend/
├── public/
│   └── index.html          # HTML шаблон
├── src/
│   ├── components/
│   │   ├── auth/           # Login & Register
│   │   ├── admin/          # Users Management
│   │   ├── dashboard/      # User Dashboard
│   │   └── common/         # Navbar, ProtectedRoute
│   ├── context/
│   │   └── AuthContext.jsx # Глобално auth state
│   ├── services/
│   │   ├── authService.js  # Auth API calls
│   │   └── adminService.js # Admin API calls
│   ├── App.jsx             # Main component с routing
│   ├── App.css             # Глобални стилове
│   └── index.js            # Entry point
└── package.json
```

## 🎯 Основни функционалности

### Автентикация
- **Регистрация** (`/register`) - създаване на нов акаунт с валидация
- **Login** (`/login`) - вход в системата с JWT токен
- **Logout** - изход от системата

### Dashboard
- **User Dashboard** (`/dashboard`) - лична страница след login

### Admin Panel
- **Users List** (`/admin/users`) - списък с всички потребители
- **Edit User** (`/admin/users/:id/edit`) - редактиране на потребител
- **Activate/Deactivate** - управление на статус
- **Delete User** - изтриване
- **Reset Password** - промяна на парола

## 🔐 Защитени маршрути

Всички страници освен `/login` и `/register` са защитени и изискват автентикация. При опит за достъп без токен, потребителят се пренасочва към login страницата.

## 🌐 API Endpoints

Frontend комуникира със следните endpoints:

### Auth Service (port 8081)
- POST `/api/auth/register` - регистрация
- POST `/api/auth/login` - вход
- POST `/api/auth/validate` - валидация на токен

### Admin Service (port 8082)
- GET `/api/admin/users` - всички потребители
- GET `/api/admin/users/{id}` - конкретен потребител
- PUT `/api/admin/users/{id}` - обновяване
- DELETE `/api/admin/users/{id}` - изтриване
- POST `/api/admin/users/{id}/activate` - активиране
- POST `/api/admin/users/{id}/deactivate` - деактивиране
- POST `/api/admin/users/{id}/reset-password` - нова парола

## 🎨 Стилове и UI

- **Gradient дизайн** - модерен изглед с градиенти
- **Responsive** - работи на всички устройства
- **Toast notifications** - уведомления за успех/грешка
- **Loading states** - индикатори при зареждане
- **Emoji icons** - визуално привлекателни икони

## 🔧 Настройки

### Промяна на API URL

Ако backend сървърите работят на други портове, променете в:
- `src/services/authService.js` - AUTH_API_URL
- `src/services/adminService.js` - ADMIN_API_URL

### CORS конфигурация

Backend сървърите трябва да разрешават заявки от http://localhost:3000

## 📦 Production Build

```bash
npm run build
```

Създава оптимизирана версия в папка `build/`

## 🐛 Troubleshooting

### Проблеми с CORS
Уверете се, че backend има правилна CORS конфигурация.

### Портове
- Frontend: 3000
- Auth Service: 8081
- Admin Service: 8082

### JWT Token
Токенът се записва в localStorage и автоматично се добавя към всяка заявка.

## 👥 Тестови потребители

След регистрация можете да влезете с вашите credentials. За админски достъп, промените ролята в базата данни на 'ADMIN'.

## 📝 Забележки

- Паролата трябва да отговаря на изискванията: 8+ символа, главна и малка буква, цифра и специален символ
- JWT токенът е валиден 24 часа
- При logout токенът се изтрива от localStorage
